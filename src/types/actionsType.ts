export enum Actions {
    INIT = "INIT",
    INTRO = "INTRO",
    GUIDE = "GUIDE",
    RANKING = "RANKING",
    GAME_PLAY = "GAME_PLAY",
    GAME_FINISH = "GAME_FINISH",
}

export enum GameActions {
    STAND_BY = "STAND_BY",
    START = "START",
}

export enum AlienActions {
    PLAYING = "PLAYING",
    NEXT = "NEXT",
}
